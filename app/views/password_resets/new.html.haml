- provide(:title, "Forgot password")
%h1 パスワード再設定
.row
  .col-md-6.col-md-offset-3
    = form_with(url: password_resets_path, scope: :password_reset, |
      local: true) do |f|                                          |
      = f.label :email
      = f.email_field :email, class: 'form-control'
      = f.submit "パスワード再設定", class: "btn btn-dark text"
