%p
  #{@user.name}様

パスワードを再設定するためには下記のリンクをクリックしてください:

\#{edit_password_reset_url(@user.reset_token, email: @user.email)}

%p このリンクは２時間で無効化されます。
%p
  もしパスワード再設定に心当たりがない場合、このメールは無視してください。パスワードは変更されないままとなります。
